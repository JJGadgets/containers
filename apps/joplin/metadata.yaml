---
app: joplin
base: false
channels:
  - &c
    name: stable
    platforms:
      - "linux/amd64"
    stable: true
    tests:
      enabled: false
      # type: web
  # - <<: *c
  #   name: beta # TODO: disabled because... Joplin's Docker image versions are always beta?
  #   stable: false
