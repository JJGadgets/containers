---
app: jjgadgets-error-page-ingress-nginx
base: false
channels:
  - name: stable
    platforms:
      - "linux/amd64"
      # - "linux/arm64"
    stable: true
    tests:
      enabled: false # TODO: "OCI runtime exec failed: exec failed: unable to start container process: exec: "sh": executable file not found in $PATH: unknown"
      # type: web
