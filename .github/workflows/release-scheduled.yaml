---
name: Scheduled Release

concurrency:
  group: container-release
  cancel-in-progress: false

on:
  workflow_dispatch:
    inputs:
      appsToBuild:
        description: App(s) to build
        required: false
        type: string
        default: all
      force:
        description: Force rebuild
        type: boolean
        default: false
        required: true
  schedule:
    - cron: "0 * * * *"

jobs:
  simple-checks:
    name: Simple Checks
    uses: JJGadgets/containers/.github/workflows/simple-checks.yaml@dba81a107b3cc23abf88eddd256a39a37cb2948b # main

  build-images:
    name: Build Images
    needs: simple-checks
    uses: JJGadgets/containers/.github/workflows/build-images.yaml@dba81a107b3cc23abf88eddd256a39a37cb2948b # main
    secrets: inherit
    permissions:
      contents: read
      packages: write
    with:
      appsToBuild: ${{ inputs.appsToBuild }}
      force: ${{ inputs.force == true }}
      pushImages: true
      sendNotifications: true

  render-readme:
    name: Render Readme
    needs: build-images
    uses: JJGadgets/containers/.github/workflows/render-readme.yaml@dba81a107b3cc23abf88eddd256a39a37cb2948b # main
    secrets: inherit
